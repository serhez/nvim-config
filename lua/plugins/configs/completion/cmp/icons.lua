local icons = require("icons")

local M = {
    Text = icons.text,
    Method = icons.cube,
    Function = icons.func,
    Constructor = icons.settings,
    Field = icons.add_tag,
    Variable = icons.greek.alpha,
    Class = icons.shapes,
    Interface = icons.three_children_tree,
    Module = icons.box,
    Property = icons.add_tag,
    Unit = icons.ruler,
    Value = icons.one_two_three,
    Enum = icons.a_to_z,
    Keyword = icons.key,
    Snippet = icons.pointy_brackets,
    Color = icons.color_palette,
    File = icons.file.filled,
    Reference = icons.exit,
    Folder = icons.folder.default,
    EnumMember = icons.a_to_z,
    Constant = icons.greek.pi,
    Struct = icons.dropdown,
    Event = icons.filled_lightning,
    Operator = icons.plus_minus,
    TypeParameter = icons.letter,
}

return M
